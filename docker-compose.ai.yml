services:
  ai-runtime:
    build:
      context: .
      dockerfile: runtime/Dockerfile
    image: alphasoft-ai-runtime:local
    env_file:
      - path: ./runtime/.env
        required: false
    ports:
      - "8100:8100"
